#pragma once

namespace Ember {

	enum class InputDevice
	{
		None = 0,
		Keyboard = 1,
		Mouse = 2,
		Gamepad = 3		// TODO: May need to extend to support multiple gamepads, but not sure best way to go about it yet
	};

}